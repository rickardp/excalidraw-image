{
  "name": "excalidraw-image",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Convert Excalidraw files to SVG/PNG from a single native binary.",
  "license": "MIT",
  "author": "Rickard",
  "scripts": {
    "lint": "eslint --no-error-on-unmatched-pattern \"src/**/*.{mjs,ts,mts}\"",
    "test": "vitest run",
    "build": "node src/scripts/build-core.mjs",
    "audit": "node src/scripts/metafile-audit.mjs",
    "test:snapshot:update": "echo 'T-006: regenerate tests/fixtures/text-wrapped.expected.json by running the capture driver. Manual for now; the full baseline-update workflow lands with FNT-005 (Playwright oracle).' && exit 1"
  },
  "dependencies": {
    "@excalidraw/excalidraw": "0.18.1",
    "esbuild": "0.28.0",
    "fontkit": "2.0.4",
    "linkedom": "0.18.12"
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "fonteditor-core": "^2.4.1",
    "globals": "^17.5.0",
    "vitest": "4.1.5",
    "wawoff2": "^2.0.1"
  }
}
