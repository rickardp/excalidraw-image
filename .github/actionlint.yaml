# Tell actionlint that `macos-15-intel` is a valid runner label. It is
# used here as the x86_64 macOS native runner; actionlint may lag GitHub's
# hosted runner label set.
self-hosted-runner:
  labels:
    - macos-15-intel
